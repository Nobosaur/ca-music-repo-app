<%= form_for(@trackEdit) do |f| %>  
    <div class="field"> 
      Name:<br><%= f.text_area :name %><br>
       Minutes: <br><%= f.text_area :minutes %><br> 
    </div> 
    <div class="actions"> 
      <%= f.submit "Edit" %> 
    </div> 
<% end %>